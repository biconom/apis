syntax = "proto3";

package biconom.type;

option go_package = "github.com/biconom/go-genproto/biconom/type/until;until_pb";

message Until {
  bool infinity = 1; // без ограничения
  uint32 until_at = 2; // значение ограничения времени действия
}
